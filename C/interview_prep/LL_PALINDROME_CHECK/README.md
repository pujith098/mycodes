# 📘 Linked List Palindrome Checker — README

This project implements:

- A **singly linked list**
- A **robust palindrome checker**
- **Loop detection**
- **Automatic test-case generation**
- Separated modular C files:
  - `linked_list.h`
  - `linked_list.c`
  - `test_generator.c`

The test generator creates random strings (palindrome & non-palindrome) and builds linked lists automatically for testing.


# 🛠️ Build & Run

## 1️⃣ Compile

Run this command inside the project directory:

gcc linked_list.c test_generator.c -o test_palindrome

./test_palindrome

